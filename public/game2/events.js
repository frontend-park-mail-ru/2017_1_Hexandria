(function(window) {
    window.EVENTS = {};

    window.EVENTS.TURN = {
        START_TURN: 'EVENTS.TURN.START_TURN'
    };

    window.EVENTS.KEYBOARD = {
        ENTER_PRESSED: 'EVENTS.KEYBOARD.ENTER_PRESSED'
    };

})(window);